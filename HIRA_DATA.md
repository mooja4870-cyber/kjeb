# HIRA(건강보험심사평가원) 데이터 연동 현황 (Phase C)

## 아키텍처
- Cloudflare Worker에서는 `apis.data.go.kr` 도달 불가(국외 egress → 504). 따라서 **KR에서 접근 가능한 로컬 러너(`hira_sync.js`)가 HIRA를 받아 D1 `hira_match_cache`에 적재**하고, Worker(`/api/hira`)는 **D1 캐시만 읽어** 매칭한다. (HIRA 기본정보는 변동이 느려 배치 동기화가 정석)
- 신원 매칭: 네이버 병원(이름+좌표) → HIRA 후보(동 단위) 중 **이름 정확/부분 일치 + 좌표거리**로 신뢰도(높음<400m / 중간 / 낮음) 산정. **낮음·이름 불일치는 matched:false**로 반환해 오귀속을 막는다(방어성 핵심).

## 동기화 방법
```bash
node hira_sync.js 주엽동 마두동 장항동 ...   # 동 이름들 → D1 적재
```
주기 동기화는 GitHub Actions(KR 러너)로 확장 가능.

## ✅ 현재 연동된 데이터 (병원기본정보 hospInfoServicev2)
요양기호(ykiho) 기준, 카드의 "🏛️ 심평원 객관정보"에 표시:
- 종별(clCdNm), 설립일→운영연수, 등록 의사 수(drTotCnt), 검증 주소, 좌표
- 출처·기준시점(asOf)·좌표오차 함께 표기 → 감사/이의제기 근거 제시 가능

## ✅ 추가 연동 — 비급여 진료비 (2026-06-23)
- 서비스: `nonPaymentDamtInfoService/getNonPaymentItemHospDtlList` (현재 키로 동작 확인)
- `ykiho`로 조인. 행정수수료(제증명)·예방접종·교육상담 제외하고 **치료성 항목만**, 가격 높은 순 상위 8개를 `hira_sync.js --nonpay <동>`이 D1 `hira_nonpay`에 적재.
- 카드에 "💰 비급여 진료비(정부 공개)" 블록으로 표시(임플란트·레진 등). 출처·환자부담·"비싸면 과잉진료/바가지 참고" 명시.
- 한계: **신고 병원만 데이터 존재**(주엽동 143곳 중 4곳만 치료성 비급여 보유) → 없으면 블록 미표시(중립).

## ⛔ 미연동 — 처방률·적정성평가 (활용신청 필요, 조사 완료)
- **서비스**: 건강보험심사평가원_병원평가정보서비스 (data.go.kr `15094093`)
- **엔드포인트**: `http://apis.data.go.kr/B551182/hospAsmInfoService/getHospAsmInfo`
- **지표**: 급성상기도감염 **항생제 처방률**, **주사제 처방률**, 적정성평가 등급(뇌졸중/혈액투석/고혈압/당뇨/천식/COPD 등). **암호화 요양기호로 조인**(우리 ykiho와 동일 체계).
- **관문**: 현재 인증키로 호출 시 `Unexpected errors` → **이 서비스 활용신청 미승인** 상태. 다른 HIRA 서비스(병원정보/상세/비급여)는 정상이므로 키 자체는 유효.
- **할 일(사용자)**: data.go.kr 로그인 → "병원평가정보서비스(15094093)" **활용신청**(개발계정 즉시 승인, 동일 인증키). 승인 후 응답 필드 확인 → `hira_sync --asm` 추가 + `hospital_metrics` 적재 + 카드 블록(비급여와 동일 패턴). 
- 참고 문서: data.go.kr 15094093 / opendata.hira.or.kr 활용가이드(병원평가정보서비스).

| 지표 | 상태 |
| :--- | :--- |
| 항생제·주사제 처방률 | 서비스·엔드포인트 확정 / **활용신청 대기** |
| 적정성평가 등급 | 동일 서비스에 포함 / **활용신청 대기** |
| 행정처분/현지조사 이력 | 심평원 아님(보건복지부/파일 데이터) → 별도 소싱 필요 |

→ 이 지표들이 들어와야 Phase D(객관지표 중심 합성 점수 objective-v3)가 완성된다. 그 전까지 카드는 "추가 데이터셋 연동 예정"으로 정직하게 고지.

## 관련 파일
- `hira_sync.js` — 로컬 동기화 러너 (curl 기반, IPv4)
- `functions/api/hira.js` — 매칭 Worker (D1 캐시 읽기 전용)
- `migrations/0004_hira.sql` — `hira_match_cache`, `hospital_metrics`
- 클라이언트 `loadHira()` — 카드 펼침 시 고신뢰 매칭만 표시
