# 추천 산식 사양 (FORMULA_SPEC)

추천 "착한병원" 판정 산식의 버전·근거·기준을 기록한다. 근거 제시(병원 이의제기 대응)와 감사(audit)를 위한 단일 출처.

---

## reviewer-v1 (현재 앱 적용 중)

- 출처: 네이버 블로그·카페·지식인 공개글, 병원명 포함 글의 긍·부정 키워드 빈도
- 등급: `pos≥3 & neg=0 → S(우수)` · `pos>neg → A(양호)` · `pos=neg → B(참고)` · `neg>pos → D(미표시)` · `pos=neg=0 → 정보부족(미표시)`
- 광고/협찬(체험단·원고료 등) 글은 집계 제외
- 한계: 긍정 키워드 대부분이 '일반 만족도'(친절·좋았)라 '과잉진료 없음'과 직접 무관

## Phase B 검증 결과 (2026-06-23, 층화 무작위표본 n=34, 추천 20곳, 95% 부트스트랩 CI)

| 지표 | 값 (95% CI) | 해석 |
| :--- | :--- | :--- |
| 추천 중 **과잉진료 직결 근거 보유** | **20% (5~40%)** | ★ 타당도 낮음 — 추천 5곳 중 1곳만 실제 근거 |
| 추천 중 광고성 글 동시 존재 | 10% (0~25%) | 협찬 오염 잔존 |
| 추천 중 표본 빈약(matched<10) | 10% (0~25%) | 통계적 신뢰 부족 |
| 재조회 시 등급 변동률 | 8% (12곳 중 1곳) | 네이버 응답 변동성 |

검증 스크립트: `validate_phase_b.py` (seed=7, 재현 가능). 표본 한계: n=34(중간 규모, CI 넓음), 일부 지역은 네이버 지역검색 throttle로 표본 미수집(부산 해운대 0곳) — 커버리지 편차 존재.

결론: **reviewer-v1 추천은 '과잉진료 없음'의 약한 대리지표.** 후기만으로는 방어력이 부족하며, 객관지표(HIRA) 결합이 필요(Phase C/D).

## reviewer-v2 (✅ Phase D에서 구현·적용 완료 — 현행 앱)

후기 기반을 유지하되 타당도·신뢰도를 높이는 기준. `gradeFromMentions()` + `/mentions`의 `ot`(과잉진료 직결 긍정글 수).

1. **최소 표본수**: `matched ≥ 10` 미만이면 추천 보류 → '정보 부족'(중립).
2. **S(우수) 승급 요건 강화**: '과잉진료 직결 근거'(착한·양심·강요없·정직·자연치아보존 등) `ot ≥ 1` 필수. 일반 칭찬만으로는 S 금지 → A.
3. **광고오염 가드**: 광고성 글 수 `ad ≥ pos`이면 추천 보류.
4. **보류 = '정보 부족'(중립)** 으로 표기. '미달'을 부정적으로 공표하지 않음 (Phase A 정책 유지).

검증(주엽동 치과 실측): 사과나무 S(ot1) / 일산연 v1의 S→**A 강등**(ot0) / 함박웃음 v1의 S→**보류**(ad10) / 바른이 **보류**(matched3). 광고·일반칭찬·표본빈약 추천이 제거되어 방어력 상승.

## objective-v3 (Phase C/D 목표 — HIRA 객관지표 중심)

- HIRA 공개 객관지표(항생제·주사제 처방률, 적정성평가, 비급여 비중, 행정처분)를 동종·동지역 백분위로 정규화 → 가중 합성 '과잉진료 위험 점수'
- 후기(reviewer-v2)는 보조 신호로 강등
- 병원별 감사로그(reco_audit)에 출처·기준시점·원값·산식버전 기록 → 이의제기 시 근거 제시
- 가중치는 본 검증 방법론(부트스트랩)으로 주기적 재보정
